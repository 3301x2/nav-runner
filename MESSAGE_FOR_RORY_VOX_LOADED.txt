Hi Rory,

Vox consent-inclusion audience is loaded and promoted end-to-end. Both tables are live and Meta-ready.

── Load summary ──

Source CSV: gs://incoming_vox/VOX_CONSENT_InclusionA_20260706_FB.csv

Sandbox table:  fmn-sandbox.staging.vox_consent_inclusiona_meta_audience
Prod table:     fmn-production-462014.staging.vox_consent_inclusiona_meta_audience

Row counts:
  • Sandbox:  132,050
  • Prod:     132,050
  • Expected: ~130,000 (delta 1.58%, within ±5% tolerance)

── Sense-checks passed ──

  • All 18 columns loaded as STRING (standard FB Custom Audience schema)
  • Every row has at least one identifier (email / phone / madid / uid) — 0 rejected-by-Meta rows
  • Emails are valid 64-char SHA-256 hex hashes
  • Phones are valid SHA-256 hex hashes
  • Zero duplicate UIDs
  • Row counts match perfectly between sandbox and prod

── Status ──

  ✓ Sandbox: loaded and validated
  ✓ Prod: bq cp from sandbox completed on first attempt, row counts match
  ✓ Ready for LiveRamp / Meta activation

── One reminder for next time ──

For any future FB audience CSV, I can now load + promote to prod with one command. Files drop into a bucket, I run one script, both tables live in ~2 minutes.

Thanks for sorting the prod access — it made this go smoothly.

Prosper
